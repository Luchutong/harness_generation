# 录制产物

`simple/` 是测试使用的固定 SFG/FT/生成样例；`deepseek_real/` 是流水线文档
引用的真实模型运行记录。两者保持原路径，以保证测试和文档中的 provenance
可复核。其余 `artifacts/*` 是本地运行数据，默认被 Git 忽略。

新实验请写入仓库根目录的 `runs/` 或自行指定外部输出目录。需要纳入版本控制的
最小复现材料放进 `tests/fixtures/`，并附来源与度量口径；不要提交 API key、
临时编译产物或整批本地 fuzz 运行目录。
