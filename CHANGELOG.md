# Changelog

## 0.1.0（待发布）

- 提供 SFG → FT → ProtocolIR → 四阶段 harness 生成与验证路径，以及单文件候选/反馈实验入口。
- 协议事实、项目函数对账、构建与运行失败记录、目标覆盖测量均保留可审计产物。
- 提供固定 mini_parser 对照、三次独立生成方差及一次源码直喂基线的录制证据。
- 整理入口文档、模块边界和产物目录规则。

已知范围：ProtocolIR 的 C 块需要显式 LLM 推断；生成质量依赖模型及目标项目，
仓库中的测量只描述对应 benchmark 和预算。CLI 的真实生成模式需要外部模型
配置，构建与 fuzz 需要本地 Clang/libFuzzer 工具链。
